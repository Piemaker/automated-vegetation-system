const express = require('express')
const app = express()
const helpers =  require('../bin/helpers')
const Temperature = require('../models/temperature')
//const dummyData = helpers.generateDummyData(0,100)
//console.table(dummyData)
//elpers.purgeModel(Temperature)
//const dummyData = helpers.dummyDateAndValue(new Date(2020,1,15),30,0,100,250)
//helpers.insertDummy(dummyData,Temperature)





















module.exports = app